# n8n + Claude — automated weekly dev summary

An n8n workflow that every Friday at 5pm pulls a GitHub repo's weekly activity
(commits, closed issues, merged PRs) and posts a narrative summary generated by
the Claude API (claude-sonnet-4-20250514) to a Slack webhook.

## Setup (5 steps)

1. **Import** — in n8n, *Workflows → Import from File* and select
   `weekly-dev-summary.json`.
2. **Configure** — open the **Settings** node and fill the values:
   - `repo` — GitHub owner/repo, e.g. `claude-builders-bounty/claude-builders-bounty`
   - `githubToken` — GitHub personal access token (repo read scope is enough)
   - `anthropicKey` — Anthropic API key
   - `slackWebhook` — Slack incoming webhook URL
   - `language` — `EN` (English) or `FR` (French)
   - `days` — window length (default 7)
3. **Activate** — toggle the workflow Active switch. The Cron node fires
   every Friday at 17:00; use "Execute workflow" to test immediately.
4. **Check the destination** — the summary lands in your Slack channel.
5. **(Optional) Switch destination** — replace the final HTTP Request node with
   an Email (SMTP) or Discord webhook node; only the last node changes.

## How it works

`Cron (Fri 17:00)` → `Settings` → `Compute Window` (since/until timestamps) →
three parallel GitHub API calls (commits / closed issues / merged PRs) →
`Build Prompt` aggregates the data and builds a language-aware Claude prompt →
`Claude API` (`POST https://api.anthropic.com/v1/messages`) → `Slack Webhook`.

## Notes

- The workflow uses expressions (`{{ $json... }}`), so credentials stay in the
  Settings node — nothing is hard-coded elsewhere.
- GitHub `since`/`until` filtering is used for commits; issues/PRs use recent-
  activity ordering. Adjust `per_page` if your repo is very busy.
- Requires n8n ≥ 1.x.
